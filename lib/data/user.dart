class User {
  final int? id;

  String? name, email, password, noTelp, fullName;

  User(
      {this.id,
      this.name,
      this.email,
      this.fullName,
      this.noTelp,
      this.password});
}
